{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "publish": "lerna publish",
    "build:core": "lerna run build --scope=@materials-ui/core",
    "build:site": "lerna run build --scope=@materials-ui/site",
    "start:core": "lerna run start --scope=@materials-ui/core",
    "start:site": "lerna run start --scope=@materials-ui/site"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GMV-centravet/materials-ui.git"
  }
}
