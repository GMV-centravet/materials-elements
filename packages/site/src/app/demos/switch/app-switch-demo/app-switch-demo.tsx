import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-switch-demo',
          styleUrl: 'app-switch-demo.scss'
        })
        export class AppSwitchDemo {

          render() {
            return (
              <Host>
                TODO: Add switch demos
              </Host>
            );
          }
        }