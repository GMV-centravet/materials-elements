import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-time-field-demo',
          styleUrl: 'app-time-field-demo.scss'
        })
        export class AppTimeFieldDemo {

          render() {
            return (
              <Host>
                TODO: Add time-field demos
              </Host>
            );
          }
        }