import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-snackbar-demo',
          styleUrl: 'app-snackbar-demo.scss'
        })
        export class AppSnackbarDemo {

          render() {
            return (
              <Host>
                TODO: Add snackbar demos
              </Host>
            );
          }
        }