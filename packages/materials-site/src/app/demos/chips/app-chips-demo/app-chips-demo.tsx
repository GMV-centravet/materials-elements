import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-chips-demo',
          styleUrl: 'app-chips-demo.scss'
        })
        export class AppChipsDemo {

          render() {
            return (
              <Host>
                TODO: Add chips demos
              </Host>
            );
          }
        }