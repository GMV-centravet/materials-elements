import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-headline-demo',
          styleUrl: 'app-headline-demo.scss'
        })
        export class AppHeadlineDemo {

          render() {
            return (
              <Host>
                TODO: Add headline demos
              </Host>
            );
          }
        }