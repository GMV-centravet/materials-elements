import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-card-action-demo',
          styleUrl: 'app-card-action-demo.scss'
        })
        export class AppCardActionDemo {

          render() {
            return (
              <Host>
                TODO: Add card-action demos
              </Host>
            );
          }
        }