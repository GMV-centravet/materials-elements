import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-select-demo',
          styleUrl: 'app-select-demo.scss'
        })
        export class AppSelectDemo {

          render() {
            return (
              <Host>
                TODO: Add select demos
              </Host>
            );
          }
        }