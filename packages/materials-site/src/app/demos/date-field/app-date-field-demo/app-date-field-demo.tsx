import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-date-field-demo',
          styleUrl: 'app-date-field-demo.scss'
        })
        export class AppDateFieldDemo {

          render() {
            return (
              <Host>
                TODO: Add date-field demos
              </Host>
            );
          }
        }