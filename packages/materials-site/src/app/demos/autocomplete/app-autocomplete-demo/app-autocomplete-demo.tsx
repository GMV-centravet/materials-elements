import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-autocomplete-demo',
          styleUrl: 'app-autocomplete-demo.scss'
        })
        export class AppAutocompleteDemo {

          render() {
            return (
              <Host>
                TODO: Add autocomplete demos
              </Host>
            );
          }
        }