import { Component, h, Host } from '@stencil/core';
        @Component({
          tag: 'app-text-field-demo',
          styleUrl: 'app-text-field-demo.scss'
        })
        export class AppTextFieldDemo {

          render() {
            return (
              <Host>
                TODO: Add text-field demos
              </Host>
            );
          }
        }